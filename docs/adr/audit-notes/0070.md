# ADR-0070 — audit notes

*Verified 2026-08-28. ADR-level verdict: `complete`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Split the single Decision paragraph into 3 ### Decision N subsections - every word preserved, only sentence boundaries promoted; it held three separately citable rules and was unciteable as one block - then appended Amendment 1.
Anchors [[],[],[]] -> [['*','1'], 1-3, 1-3].
ALL THREE DECISIONS HOLD in code, at the exact choke points the ADR names: dec.
1 EffectTimeline.reset() :227-233 re-applies _rng.seed = _rng_seed FIRST, before the subsystem resets, citing ADR-0070; dec.
2 EffectInstance.gd:266 sets manager.rng BEFORE initialize (the ordering the ADR calls out), :335 camera_controller.rng, ParticleSubsystem.gd:111 forwards to physics, and ParticlePhysics rand/rand_range/_srange + CameraSubsystem._rr all carry the documented null->global fallback; dec.
3 _advance_one_frame() :102 is the single frame body shared by tick() :85 and seek() :132, whose docstring names the exact drift the ADR reasoned about.
The Implementation notes are accurate line for line.
The scope boundary is still true (TrapEffect, TrapChargeLineEffect, TrailProjectileCallback are the global-RNG sites); the one draw it does not name - ColorSubsystem.gd:86 owner_id = randi() - is INERT: an identity token keyed into MapTintOverlay.active_layers, never reaching a pixel, assigned from initialize() not reset(), so a backward seek does not re-roll it.
Recorded so the next audit does not re-open it.
The tree grew three things the ADR does not mention, all consistent with it: EffectTimeline.rescrub() (reset + re-pump to the CURRENT frame, for authoring edits to folded channels - seek(effect_frame) folds nothing), EffectInstance.seek_silent() :739 used by the Studio backward loop, and EffectEndModel.derived_end_frame() which discharges the 'reproducible for testing generally' consequence (sets mgr.rng + tl.set_rng, 'so the harness re-seeds identically to the live cast').
Its fixed MARKER_SEED=0 does NOT conflict with 'across casts gameplay still varies' - the number is the Studio score timeline's end marker (EffectScoreTimeline.gd:404, EffectStudioPage.gd:7374), an authoring display value, never gameplay.
ONE STALE REFERENCE: the guards list cites on-screen verification via tools/StudioPreviewProbe.tscn, which has ZERO files in the tree AND no add/delete anywhere in git history - it was never tracked, not deleted by ADR-0069's rehost (corrected 2026-08-28 during the ADR-0069 fold).
A casualty of the neighbouring ADR's rehost, not of this design; both automated guards it names survive (tests/EffectSeekTest.gd, tests/EffectRngDeterminismTest.gd).
No ambiguity to record.
Guard feasibility: already well mechanized - EffectRngDeterminismTest covers dec.
1+2 end to end (helpers, a full cloud, camera shake, a back-and-forth scrub reproducing the exact frame-N cloud) and EffectSeekTest covers dec.
3.
The uncovered arm is dec.
2's COMPLETENESS: nothing stops a new sim draw reaching for the global randf().
A grep guard is possible - no bare randf()/randf_range()/randi() in src/effects/ with an allowlist for the four documented boundary files - and would keep the scope boundary honest as the runtime grows; its cost is the allowlist, the same burn-down shape check_par_shaders.py already carries.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

Already mechanized, and well: `EffectRngDeterminismTest` asserts the physics
helpers, a full particle cloud, camera shake, and a back-and-forth scrub
reproducing the exact frame-N cloud — that is dec. 1 and dec. 2 end to end —
while `EffectSeekTest` covers the seek pump and the reset re-seed for dec. 3.

The one arm not covered is dec. 2's *completeness*: nothing stops a new
stochastic draw in the sim from reaching for the global `randf()`. A grep-shaped
guard is possible — no bare `randf()` / `randf_range()` / `randi()` in
`src/effects/`, with an allowlist for the four documented boundary files
(`TrapEffect`, `TrapChargeLineEffect`, `TrailProjectileCallback`, and
`ColorSubsystem`'s `owner_id`) — and it would be the arm that keeps the scope
boundary above honest as the runtime grows. Its cost is the allowlist, which is
the same burn-down shape `check_par_shaders.py` already carries.
