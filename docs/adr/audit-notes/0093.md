# ADR-0093 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Four Decision paragraphs numbered (### Decision 1-4); the bare '## Amendment' became '## Amendment 1' (nothing in the corpus cited it).
Anchors ['*'] -> ['*','1','2'] + dec/sec 1-4.
The Decision is built to the letter and well guarded: global bottom-pinned lane (EffectScoreModel._pacing_lane :277 'global':true, laid out LAST at EffectScoreTimeline:766), pacing_norm slowness map + greyed-when-disabled, the outline+per-column-rect revision (pacing_top_points :1899 / pacing_fill_rects :1924, arms at EffectScoreTimelineTest:1602/1642/1656), pop-up painter at PACING_VMAX=10 with played-window scope and whole-600-int per-stroke undo (TimeScaleChannel duplicates before_raw), and the transport accumulator (EffectStudioPage:3960 -> pacing_factor_at :347).
Head Status still says '#270 /tdd build pending' — 5th audited ADR in a row (0040, 0069, 0071, 0092, 0093) advertising shipped work as pending.
AMENDMENT 1 IS UNBUILT IN EVERY CLAUSE and its own Status honestly says so — no accordion, no focus/dim, no bare-drag paint, axis still 0-10, enables still in the painter panel head (:1008-1012) not the gutter; one clause ('gains a gutter name') was already true since the original build.
Its CONSEQUENCE clause is present-tense and false: '_open_pacing_painter ...
are deleted' — it is defined at EffectStudioPage:6272 and is the ONLY route for a band click (:4243).
Landing it also requires rewriting arms 2-4 of the green EffectStudioTimeScaleAcceptanceTest, which drive the pop-up path.
Consequences bullet 1's 'those bits are surfaced ONLY on the timeline path' is FALSE — EffectSettingsProjector._flags_section still declares 0x20/0x40 and EffectSettingsProjectorTest:183-196 asserts them (see 0092).
One writer, two controls.
Corpus stats (~94% value 2, 15 real 10s, only 2-10 occur) are UNVERIFIABLE here — assets/effects is gitignored and unpopulated; unverified, not refuted.
RECORDED QUESTION: (A) Amendment 1 still binds and is #270's remaining work (delete the pop-up, rewrite the acceptance arms), or (B) the shipped pop-up is the answer and the amendment's present-tense prose should be restated as a proposal or withdrawn.
Same control-home call ADR-0092 is holding; not guessed.
Guard arm identified but not written: 'a symbol an ADR declares deleted is absent from src/' — seeded here it reports _open_pacing_painter and fails, correctly, while Amendment 1 is pending.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

Decisions 1–3 and the transport consequence are already covered (`EffectScoreModelTest`,
`EffectScoreTimelineTest`, `EffectStudioTimeScaleAcceptanceTest`). The arm that is **absent and
writable today** is a doc-vs-tree one, not a behaviour one: assert that a symbol an ADR
declares "deleted" is in fact absent from `src/`. Seeded against this ADR it would report
`_open_pacing_painter` at `EffectStudioPage.gd:6272` and fail, which is the correct answer while
Amendment 1 is unbuilt — so it can only be written after the question above is settled, or
written to read Amendment 1's Status ("build pending") as an explicit exemption.

## 2026-08-28 — blocked

Taken as the burn-down head after ADR-0092 blocked, and blocked for the same reason. Amendment 1
is unbuilt in every clause, its consequence prose is present-tense and false (`_open_pacing_painter`
is defined at `EffectStudioPage:6272` and is the only route for a band click at `:4243`), and
landing it would require rewriting arms 2–4 of the green `EffectStudioTimeScaleAcceptanceTest`.
Withdrawing it instead means editing this ADR's own Consequences bullet 1 ("surfaced **only** on
the timeline path", which is false today) and ADR-0092's Amendment 1.

Neither is a mechanical call, and both hang off the same product question ADR-0092 records. The
file was not rewritten. Decisions 1–4 grade `built` in the register on their own evidence.
