# ADR-0040 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Numbered the 8 Decision paragraphs (### headings, prose untouched) and appended Amendment 1.
Status still says Proposed but the ADR is BUILT and load-bearing: the autoload is registered at project.godot:42, ADR-0200 (Accepted 2026-08-21) cites 'the 24-float ADR-0040 record layout', ADR-0045's Context is entirely about it, and two tests cite it.
Dec.
2/8 hold outright; dec.
3 holds with the seam moved to a 4-arg addon include; dec.
1's FIVE MultiMeshInstance3D per effect collapsed to ONE under #227 (the four RM_MODE0..3 carriers retired, the four named effect_particle_mode{0..3}.gdshader files do not exist); dec.
4's transparent half no longer uses Godot's transparent queue at all (unified 24-float SSBO + display-space compositor fold); dec.
5's replacement was itself replaced (OTDepthPrimOrder.order + render_layer_order; submission order is now only the tie-break); dec.
6's 5xN_effects is now 1xN_effects; dec.
7 names borrow_multimesh_set() which has ZERO hits repo-wide and never existed (the API is borrow_slot/release_slot/get_multimesh/get_material/set_effect_texture/set_palette_texture).
The record grew 20 -> 24 floats at slice C.
The deferred [PARTICLE_PERF] rebracketing did not land: the log moved to :249-254 but still prints mode0..mode3 from _used_counts[1..4], which are reset at :190-191 and incremented only inside 'if rm == _RM_OPAQUE' (:358-360), so they are structurally always 0; get_active_count()'s 'across all 5 render_modes' docstring is stale on dead API (zero callers - ParticleSubsystem calls ParticlePool's same-named method).
The Verification gate (E317/E019 replay; draw_calls peak <100, spikes <30, mean <17ms vs the 2026-06-13 baseline of 19.40ms/108/573) appears in no other document and was never discharged, and is no longer measurable as written.
RECORDED QUESTION, not resolved: amended or superseded? Amended - the record layout, the instance-order argument and the borrow contract are what ADR-0200/ADR-0045 cite and all are live, so Status should become Accepted.
Superseded - the Decision's central artefact (5 per-blend-mode MultiMeshes on the transparent queue) exists in no form, and what ships is a design this ADR neither describes nor chose.
Left Proposed rather than flipping it on my own authority.
Guard feasibility: two cheap arms - assert every \w+() method name backticked in a Decision resolves to a func in src/ (borrow_multimesh_set has been fiction for 2.5 months), and assert every assets/shaders/*.gdshader path named in an ADR exists (effect_particle_mode{0..3} is named 4x here, 0x on disk).
A perf arm is not worth writing.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

Two arms are cheap and would have caught real drift:

1. **The named-API arm.** Assert that every backtick-quoted method name in an
   ADR's Decision section that looks like a call (`\w+\(\)`) resolves to a
   `func` in `src/` or is explicitly marked as never-built.
   `borrow_multimesh_set()` has been fiction in an ADR for two and a half
   months, and nothing reads it.
2. **The named-file arm.** Assert that every `assets/shaders/*.gdshader` path
   named in an ADR exists. `effect_particle_mode{0..3}.gdshader` is named four
   times here and zero times on disk.

A perf arm is not worth writing: the acceptance thresholds above are keyed to a
draw-call shape the tree no longer has.
