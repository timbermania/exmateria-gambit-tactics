# ADR-0044 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Numbered the 5 Decision bullets (additive; 0 decision anchors and 0 `ADR-0044 dec.
N` citations tree-wide before) and appended Amendment 1 grading each against the shaders.
HOLDS: dec.
1, 2 (globals), 3, 5 — psx_par_anchor() at psx_par.gdshaderinc:66-68 is byte-for-byte the Decision's code block with `stretch` in the <taxonomy>_stretch slot; project.godot declares all three floats at 1.0; CONTEXT term at 28-display-and-pixel-aspect.md:132.
The wiring guard tools/check_sprite_stretch_globals.py (run_all_tests.sh:636) ran GREEN this pass.
HALF: dec.
4 — the three live_*_stretch channels + *_changed signals are at PSXDisplay.gd:198/219/240 and :286-288, but DisplayDebugPanel is DELETED (ADR-0151, bb5f7fefa); the scrubs are generated Tunables cards bound at PSXDisplay.gd:80/83/86 (ADR-0068), and the only writer of a live_*_stretch tree-wide is TunePortTest.gd:165.
FALSE: the 'Shadows live in the Effects bucket' consequence.
shadow_blob.gdshader:68-71 is Pattern 1 (psx_par_full), no taxonomy.
It DID ship in the FX bucket at this ADR's own commit 9fec26b18; ee46272fc (2026-07-03) rebuilt it as a ground-locked ground decal and 66ecc4fb9 (2026-07-04) gave it Pattern 1.
Neither cites this ADR and NO ADR covers the ground-locked shadow; ADR-0060 lists 'unit shadow' under Pattern 1 without noting the reversal, and its context line 'the one battle mesh applying no psx_par at all' is true of the 2026-07-03 decal, not of the mesh this ADR shipped.
MEASURED DEFECT: the tile cursor's two passes read DIFFERENT globals — the opaque body via tile_cursor.gdshaderinc:64 -> psx_cursor_stretch, the STP outline via cursor_fold.gdshaderinc:50 -> psx_fx_stretch (taken at 542f418bd, 2026-07-28, ADR-0074 4c, after this ADR).
Both default 1.0 so they agree today; scrubbing psx_cursor_stretch — the knob this ADR exists to create, for the sprite it was written about — widens the body only.
Derived from the third arguments, not observed headful.
AMBIGUOUS, recorded not resolved: is dec.
2's 'everything else: particles, projectiles, shadow blobs, callback meshes' a ROSTER (then projectile_vertex_color.gdshader:30 and trap_charge_line.gdshader:17 hardcoding 1.0 are violations) or a DEFAULT FOR SPRITES (then a literal 1.0 is the right way to say 'authored geometry, no billboard width', as both shaders' own comments argue)? A roster guard cannot be written until a human rules.
Guard feasibility: check_par_shaders.py enforces the seam but never inspects the third argument, and check_sprite_stretch_globals.py proves each global has one reader, not WHICH mesh reads it — so the taxonomy roster is unguarded.
Arm 2 (assert tile_cursor.gdshaderinc and cursor_fold.gdshaderinc pass the SAME global) is unblocked and RED today under either reading.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

`check_sprite_stretch_globals.py` already guards the *wiring* (dec. 2's globals
+ dec. 4's channels), and it is deliberately path-independent — it walks
`_walk_roots.walk_roots()`, which is why `PSXDisplay.gd`'s two moves (into
`addons/exmateria_platform/display_port/` via ADR-0147 dec. 1 → ADR-0171 dec. 1
→ ADR-0184 dec. 2) never reported the seam as absent. Three arms are possible
and unwritten:

1. **The roster arm (blocked on the recorded question).** Parse the third
   argument of every `psx_par_anchor(` call and assert it is one of the three
   globals. Red today at three sites under reading A, one under reading B.
2. **The cursor-coherence arm (not blocked).** Assert that every shader reading
   the tile-cursor sprite's geometry passes `psx_cursor_stretch` — i.e. that
   `tile_cursor.gdshaderinc` and `cursor_fold.gdshaderinc` pass the *same*
   global. This is red today under either reading and asserts precisely what
   this ADR was written to make possible.
3. **The dec. 4 scrub arm.** Assert the three `render.psx_*_stretch` slugs are
   `bind_update`-registered, which is what makes them scrubbable now that
   `DisplayDebugPanel` is gone. Green today; it would have caught the deletion
   silently orphaning them.
