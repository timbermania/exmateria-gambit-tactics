# ADR-0046 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Decision items numbered 1-5 on 2026-08-28; no prior `dec.
N` citation existed to break.
Graded against the code: all five hold on the tile cursor and TileCursorBobTest asserts the 52-frame cycle against the shipped artifact — but decisions 4 and 5 are provisional BY THEIR OWN TEXT (the N=1 re-confirmation and the excursion sign both await a capture that has not landed), and no `check_*.py` names this ADR.
Amendment 1 records the two facts that MOVED: the glove half Status calls deferred is BUILT (`GloveCursorBob` + eight consumers, bob axis X, `FormationStartMenuTest` as oracle), and extraction #3 pass 4 split `cursor_bob.json` into `tile_knife.json` + `glove_cursor.json`, so decision 1 and the Consequences artifact bullet name a file that no longer exists.
Four citations are method precedent, not this decision (`parse_number_popup.py`, its test, `bootstrap_assets.sh`, `FormationScene`): they mean the ROOT `0001-iso-derived-assets-reproducible.md`, which a bare `ADR-0001` inside godot-learning misaddresses.
