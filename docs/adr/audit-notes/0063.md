# ADR-0063 — audit notes

*Verified 2026-08-28. ADR-level verdict: `complete`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

VERDICT SCOPE: 'complete' = every DECISION conforms in code; one named SCOPE item (the miss number) is unbuilt — see GAP below.
Six Decision bullets numbered (anchors [] -> ['*','1'] + dec/sec 1-6).
The most fully built ADR audited so far: dec.
1 (CombatLoop.hp_changed(unit_index, prev_hp, new_hp, delta) exists with that EXACT signature at CombatLoop.gd:37, was_heal/killed at GPUCombatInterpreter:63-64, connected in FeedbackHudManager.setup:55), dec.
2 (both billboards add_to_group('combat_visuals'), CUSTOM0/OT depth), dec.
3 (FieldInspectController -> screen-space UIUnitInfoWindow), dec.
4 verbatim (numbers parented to the MANAGER = map-anchored, bubbles to the unit), dec.
5 parser half (status_icon_set reads BATTLE.BIN 0x2D9DC/0x2D9F4 = the ADR's 0x800949dc less the 0x80067000 base), dec.
6 including the literal (UNIT_PICK_MASK := 4).
TWO THINGS THIS CORPUS HAS NOT SHOWN BEFORE: the Status is honest (no pending claim, so the 0040/0069/0071/0092/0093 stale-Status streak does not extend), and the cross-cutting rule HAS a guard — tools/check_feedback_hud.py, wired at run_all_tests.sh:665, failing on ROM reads/state writes from a HUD class and on a billboard missing the group or the OT shader.
GAP: the MISS number named three times (Status scope, dec.
2, dec.
5) was never built — DamageNumber3D ships enum Kind {DAMAGE, HEAL, MP}, there is no miss path and no CombatLoop signal that could distinguish a miss from a zero-damage hit; MP is built and named nowhere in the ADR.
Declared-in-source cosmetic shortfalls (HEAL/MP flat tints, zodiac reusing the 0x7d7c digit CLUT) match the ADR's own 'non-blocking capture items' consequence.
RECORDED QUESTION on dec.
5: the parser extracts the faithful status_type->cell table but the runtime uses FeedbackHudManager.STATUS_ICON = {poison:1 ...
slowed:5} / CHARGE_ICON:=19, declared PLACEHOLDER because the ROM-table-to-our-status reconciliation is UNRECOVERED.
(A) an open gap — an icon's identity is a graphic and dec.
5 promises faithful graphics; or (B) inside dec.
5's own 'only the look is faithful, the meaning is ours' boundary, since hasted/slowed may have no ROM counterpart and a total mapping is undefined, not merely unrecovered.
Needs a game-design answer this ADR does not own.
Guard arm identified for later: assert the bubble's cell indices come from the extracted table rather than a literal dict — correct answer today is the literal, so it waits on the question.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

Dec. 1 and dec. 2 are already covered by `check_feedback_hud.py`; dec. 4's ownership split and
dec. 6's pick mask are covered by `FeedbackHudTest` / the field-inspect path. The arm that is
**absent and cheap** is over dec. 5: assert that every cell index `FeedbackHudManager` hands to
`StatusBubble3D` is `< RangeTileAtlas.status_icon_count()` and that the mapping is sourced from
the extracted table rather than a literal dictionary. Today it would report the literal, which
is the correct answer while the question above is open — so it is a guard to write *after* the
reconciliation, not before.
