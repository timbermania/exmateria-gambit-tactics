# ADR-0016 — audit notes

*Verified 2026-08-28. ADR-level verdict: `complete`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Decision numbered 1-9 on 2026-08-28 — the ADR had NO `## Decision` section; the rules were lifted from the chosen option and the Consequences bullets with a pointer each, nothing retired.
Graded decision by decision against the code: 8 of 9 HOLD as written, including both nets (`_validate_gambit_config_schema` push_error at boot + `GambitEncodeSchemaTest` wired in run_all_tests.sh) and the whole-buffer check walking `range(GAMBIT_SIZE)` with RESERVED_14/15 and GAMBIT_SIZE still 16.
Decision 7 held in RULE but not in FILENAME: the schema, the check and `_pack_gambits` moved `GPUBatchSimulator.gd` -> `GPUCombatPacker.gd` (the same host move as ADR-0001's generated region), which made 'next to UNIT_CONFIG_SCHEMA' MORE true — all four unit/gambit schema symbols are now in one file — and broke only 'beside set_unit_gambits', which stayed at GPUBatchSimulator.gd:1245 and calls across.
No standalone layout module exists, so ADR-0001 dec.
4's deferral still stands.
Decision 9 ('behavior is unchanged') is NOT re-derivable from the current tree — it is a claim about the landing commit, and the audit records that as a limit rather than a finding.
Incidental: `GPUCombatPacker.gd:686` comments GAMBITS_PER_UNIT as '80 ints per unit' when the generated value is 96 (MAX_GAMBITS went 5 -> 6 for ADR-0048's safety net) — the value cannot drift, only the prose beside it did.
