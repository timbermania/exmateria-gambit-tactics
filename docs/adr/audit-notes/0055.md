# ADR-0055 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Numbered 7 Decision items 2026-08-28 (1-6 from the opening paragraph, 7 lifted from Consequences because it is the one that got reversed); dec.
anchors 1-7 added, amendment dated not numbered.
MEASURED: dec.
1 exact -- ScenarioMotion carries those six fields and four methods and nothing else but frac/curve.
dec.
2 rule holds but the REGISTRY MOVED: 'VM-owned motions:{uid -> ScenarioMotion}' is now actors[uid].motion (ADR-0064 folded it into ScenarioActor).
dec.
4 holds (motion_done:3410 -> _actor_motion_live else _rotate_done).
dec.
5 holds: Unit._rotate_state:315 is still the only owner and _sprite_moves/_walks are at ZERO occurrences.
dec.
6 is a LIVE deferral: none of ScenarioColorTint/BgSound/DarkScreen/Weather has advance/is_done/snap_to_end.
Both behaviour notes hold; _motion_watchdog:3437 = maxi(600, ceil(dur_s*60)+60) and ScenarioSpriteMoveTest asserts BOTH arms.
DEC.
7 IS REVERSED AND NO ADR SAYS SO: ScenarioPathMotion is a SIBLING class for {28} Walk To (per-tile gravity arc DAT_80096128=0x925 + polyline re-projection, FUN_8006af7c) which a linear easing=0 lerp cannot express.
It appears in NO ADR and NO docs/context file and cites THIS ADR as its authority while contradicting its consequence.
The CONTRACT survived the split intact -- both classes are read duck-typed at _advance_motions:3450 and _drain_motions_to_target:3781 with no call-site change, which is the strongest evidence for dec.
1 in the tree, produced by the decision it broke.
DEC.
3 IS THREE OF FOUR: {6F}/{29}/{64} call _arm_motion_wait; {65} Wait Rotate All arms _all_rotations_done instead and CANNOT use motion_done(uid) -- PSX passes a0=-1 over all 21 handles, so it has no uid.
Faithful behaviour, overstated ADR.
The source contradicts itself 17 lines apart: motion_done:3404 lists {65}, _arm_motion_wait:3420 does not.
The predicate also OUTGREW the four opcodes: the unit-filter-free {E5} Task=11 kind-0x0B liveness reads the same _actor_motion_live, added after a split-model bug let scn6's carry-down race its per-step barriers.
THE OPTION-3 REJECTION'S FAULT LINE HAS COLLAPSED: it rests on 'effect timers tick OUTSIDE the _running halt gate whereas motions tick INSIDE'.
Today motions advance in _advance_tick_visuals:1392 from _advance_frame BEFORE _tick_once (its docstring: 'Both run during a halt (_running false)'), and the effect ramps advance at _tick_once:1587 before its halt return under 'if not paused'.
IDENTICAL gating: both outside _running, both frozen by paused.
ADR-0065 did this and never mentions the halt gate, _running, paused, or ADR-0055.
Two later ADRs cite the collapsed leg by name -- ADR-0058:74 and ADR-0064:103.
RECORDED QUESTION: does the rejection still hold? (a) yes, only the wording is stale -- the load-bearing axis was always 'blocks a script context' vs not, which is untouched (motion_done gates a context, no tint does), so the fix is one sentence plus a pointer from ADR-0065; (b) no -- the ADR calls it a HARD semantic fault line and cites the gate as what makes it hard, so with both task classes on identical gating the shared self-ticking-task base is open again and must be re-argued on merits.
GUARD GAPS: dec.
5 as a negative (zero _sprite_moves/_walks in src/, _rotate_state only in Unit.gd); dec.
6 as a negative (no effect class declares advance/is_done/snap_to_end -- that IS the whole content of 'not migrated'); dec.
1 as a cross-implementation contract (every class stored in ScenarioActor.motion answers all four methods -- today reached only by duck typing, and the guard is what makes a third implementation safe).
Dec.
3 is NOT worth mechanizing as written: {65} refutes it.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

Dec. 1 and dec. 4 are already covered (`ScenarioMotionTest`,
`ScenarioPathMotionTest`, `ScenarioSpriteMoveTest`). Three arms are absent and
each is cheap:

- **Dec. 5 as a negative:** assert `_sprite_moves` / `_walks` have zero
  occurrences in `src/` and `_rotate_state` occurs only in `src/units/Unit.gd` —
  the "rotate stays on the Unit, the other two dicts are gone" invariant, which
  is exactly the shape a later refactor would silently undo.
- **Dec. 6 as a negative:** assert no `ScenarioColorTint`/`ScenarioBgSound`/
  `ScenarioDarkScreen`/`ScenarioWeather` declares `advance`/`is_done`/
  `snap_to_end`. That is the *entire* content of "not migrated", and it would
  fire the day someone folds them in without modelling the halt-gate axis.
- **Dec. 1 as a contract, across implementations:** assert that every class
  stored in `ScenarioActor.motion` answers all four methods. Today that is two
  classes reached only by duck typing; the guard is what makes a third safe.

Dec. 3 is the one arm **not** worth mechanizing as written, because the ADR's
own text is what would have to be asserted, and `{65}` refutes it.
