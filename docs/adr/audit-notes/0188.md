# ADR-0188 — audit notes

*Verified 2026-08-28. ADR-level verdict: `complete`. Written during the de-amendment
rewrite that folded three dated blocks and two inline correction blockquotes into
decisions 1–7.*

This ADR was **unaudited** — no `AUDIT.tsv` rows and no numbered decisions. The rewrite
numbers the six existing Decision bullets 1–6 in the order they already stood, so no
existing meaning moves, and appends 7. Only one citation in the corpus addressed a dated
block: `docs/context/11-campaign-spine.md:178`, `(ADR-0188 Amendment 1)` → `(ADR-0188
dec. 7)`, retargeted in the same commit. The other four ADR-0188 citations address the ADR
as a whole.

## Fold disposition

| block | class | disposition |
|---|---|---|
| `## Amendment 1 (2026-08-27)` — a screen-out is the screen's | a new decision | **promoted to decision 7**, both halves: the ADR-0172 §1 authorship reading that makes a screen-out the screen's without superseding anything, and the `CONTEXT.md` Screen-out / Host cue referent split. The "a two-axis review caught it" framing and the "the present tense here was wrong for one commit" aside are how they learned it → deleted |
| `## Amendment 2 (2026-08-27)` — the frame budget counted the wrong unit | a correction to a decision's own statement | **folded into decision 5**, which said only "a frame budget". Decision 5 now states the unit (frames without progress), the crossover argument, `STALL_FRAMES` / `screen_out_ticks()`, the `is_inside_tree()` check on both sides of the await, and the one-way latch. The `RAMP_TICKS * 8` / `* 12` bug narrative → deleted |
| `## Amendment 3 (2026-08-28)` — the third waiter, and an arm that could not fail | mixed | the two test facts → `## Verification`; the `-1` sentinel removal and `has_screen_quad()` → decision 5's closing paragraph, stated as the API rather than as a change; the two declined repairs (nulling `_screen_out`, extracting the stall loop) → a new `## Considered options` section, which this ADR did not have |
| `> Amended 2026-08-27` inside Consequences (the retired `Tune` owner manifest) | an action taken elsewhere | **deleted.** Decision 3 states the now — the class self-registers from `_static_init`, kept there by `tools/check_tune_owner_self_registration.py` |
| `> Amended 2026-08-28` inside Consequences (the *second* citation of that manifest) | an action taken elsewhere | **deleted**, same reason |
| `> Amended 2026-08-28` inside Amendment 2 ("the test", singular) | how they learned it | **deleted** |

Verified that the retired mechanism really is retired: `tools/check_tune_owner_manifest.py`
does not exist, and the only occurrences of `Tune.register_all()` / that filename in the
tree are comments in `src/core/Tune.gd`, `SkirtConfig.gd` and
`check_tune_owner_self_registration.py` describing what was deleted. The claim-diff reports
those tokens as "live" because it greps text; as code they are gone, so dropping them is
correct.

## One correction to the Status line

It claimed `tests/WorldMapScreenOutTest.gd` carries **18 assertions**; it carries **19**.
The rewrite drops the count rather than fixing it — a raw assertion tally is a number with
no design content that drifts on every edit, and `## Verification` now says what the suite
pins instead. The Status line's "three arms seeded red" for that suite is also dropped: it
is a claim about how the suite was written that nothing in the tree records, unlike
`WorldMapMountTest`'s two ✕ arms, whose seeded-red provenance *is* written in the file
(`# arm reds alone`) and is kept.

## Everything else measured as stated

Re-measured rather than trusted, and all of it holds to the literal: the out-arm table and
both endpoints against `WorldMapScreenOut.value_at`; `START_VALUE` 32, `CEIL_VALUE` 255,
`LEVEL_STEP` 8 with 255 baking to 248; `land_tick()` deriving 14; `STALL_FRAMES` 240 and
the three waiters that read it (`WorldMapScene._run_screen_out`, `WorldMapScreenOutTest`,
`WorldMapMountTest`) all converted to the progress-bounded loop; `is_inside_tree()` on both
sides of the await; the R3 write-back at `WorldMapScene._ready` with the static var left as
the authored default; the three skips written one per line; `--menu=screenout` reaching a
real branch; and `CONTEXT.md`'s Screen-out entry with the Host cue `_Avoid_` pointing at it.

## No open question

Nothing forced a reading. The one thing this ADR explicitly does *not* claim — that the
console fires the out arm at the hand-off — is stated as unclaimed by decision 2 and is the
reason the tunable exists, so it is a recorded absence rather than an ambiguity. No
`proposed_guard`: the picture half is a contact sheet a person looks at, and the arithmetic
half is already pinned.
