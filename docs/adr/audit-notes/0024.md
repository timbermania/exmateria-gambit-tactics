# ADR-0024 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Decision items numbered 1-5 on 2026-08-28; the anchor scanner read ZERO decision anchors on this file first, so no `dec.
N` citation existed to break.
Graded against the code: the shape IS built (Unit owns the map call, `Unit.gd:739/762/797`), and dec.
5 is CLOSED — `start_attack_with_animation` is defined nowhere, and both named CombatLoop sites migrated (`:1566` unit.attack, `:1721` cast_spell; the ADR's `:877`/`:979` are stale line numbers for those same two).
Two decisions are half: `resolve_spell_casting_two_hands` (dec.
2) and `resolve_ability_charging` (dec.
3) were NEVER WRITTEN, and the string ABILITY_CHARGING appears in no file under src/ or tests/ — of the three unbuilt paths the opening paragraph names, two landed and the third has neither activity nor resolver.
Dec.
1's `use_back` clause is superseded by ADR-0053: `attack` passes false unconditionally and `resolve_attack` takes it as `_use_back` (unused), the variant being picked per paint in `UnitDisplay._paint_body_variant`.
`unit.attack_with_item_effect`, named in dec.
5 as a migration target, never existed — the item path shipped as an unnamed FOURTH semantic method `Unit.use_item` (`:779`).
Gameplay reaches none of the four directly: `ActivityTranslator._translate_parameterized` (`:94`) dispatches by method-NAME STRING via `unit.call(method, param)`, so a rename breaks the GPU path with no compile error.
The panel consequence holds exactly — `UnitAnimationViewerPanel.gd` has ZERO AnimationResolutionMap references.
`Unit.gd:731-733` still says `last_resolution` is null after a parameterless change, but `update_animation` writes it on every parameterless resolve (`:905`) — the comment is the stale half, not the behaviour.
Amendment 1 also records a MIS-CITATION class not fixed here: three source files / five sites (`stage_compute.glsl:475,:1095`, `stage_pathfind.glsl:555`, `combat_common.glslinc:203,:242`) cite ADR-0024 for unit-anchored gambit MOVE / ACTION_MOVE_TO_UNIT / LOGICAL_ACTIVITY_APPROACHING, which are ADR-0062's decision — so the register's `code` count for this number is inflated, and `check_adr_anchors.py` is structurally blind to it because a bare ADR-NNNN carries no anchor.
All three References (0019/0020/0021) resolve to live accepted designs with live classes.
