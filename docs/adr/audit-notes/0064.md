# ADR-0064 — audit notes

*Verified 2026-08-28. ADR-level verdict: `complete`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

VERDICT SCOPE: 'complete' = all EIGHT decisions conform in code; dec.
8's SCOPE SENTENCE has been overtaken twice, in the good direction, and that is the recorded question.
Numbered 8 Decision items 2026-08-28 (1-4+7 from the opening paragraph, 5-6+8 from Considered options / Consequences); dec.
anchors 1-8 added, amendment dated not numbered.
MEASURED: the five old dicts (_unit_tints, _cinematic_walkers, cinematic_unit_atlas_y_offset, _unit_move_home, the loose motions) have ZERO live references in src/scenarios/ and no forwarding property was added.
Four verbs at actor:3265, peek_actor:3277, forget:3299, reset_all:3320.
forget is ONE LINE (actors.erase).
_forget_unit no longer exists as a symbol.
start:1029 and set_rewind_target:1139 are the ONLY two call sites of reset_all in src/, so the three-disagreeing-paths bug is unrepresentable at the level it was diagnosed.
Unit.reset_scenario_cutscene_state:1880 owns the Unit-side reset; the VM never pokes _rotate_state/current_anim_id.
THE REJECTED OPTION IS THE ONE WITH A TEST: ScenarioActorTest has 15 arms and _test_forget_does_not_reset_unit_fields pins the ASYMMETRY -- a considered-and-REJECTED alternative mechanized, the first such case this audit has found, and the arm most likely to matter since 'make forget and reset_all do the same thing' is exactly the tidying a later reader attempts.
Migrated seams all present: vm.actor(uid).motion and active_motion_count(), whose docstring records why a bare actors.size() cannot substitute.
DEC.
8'S SCOPE SENTENCE IS OVERTAKEN TWICE: the actor carries SIX per-unit structures (pending_anim, the {11}/{8C} Unit Anim latch mirroring PSX unit+0x0C, joined and cites 'the ADR-0064 single reset path' in its own docstring) and the Unit method resets FOUR fields (the {92} SS=1 crystal billboard joined facing_angle/_rotate_state/current_anim_id).
Both are the consolidation PAYING OUT -- new per-unit state went to the one home and inherited the one reset for free, which is exactly what the ADR argued the five loose dicts made impossible.
reset_all also gained arity: (units_by_id, preserve_poses := false), the combat->scenario pose carry; verb and meaning unchanged.
RECORDED QUESTION: is dec.
8 a bound on the CHANGE or on the ACTOR? (a) on the change -- 'this ADR keeps the actor to...' describes what landed, so nothing is violated and later state joining the actor is the ADR succeeding; (b) on the actor -- the clause shares a sentence with a genuine still-honoured exclusion (the effect-timer task-pump), which argues both halves were meant to constrain forward, and pending_anim citing this ADR while exceeding it means the GROWTH RULE is undocumented: nothing says what MAY join the actor, only what did.
Measured, the exclusion itself still holds -- no effect timer is on the actor and none of ColorTint/BgSound/DarkScreen/Weather adopted the task contract (= ADR-0055 dec.
6).
NB its stated REASON ('it reopens the ADR-0055 halt-gate fault line') is the premise ADR-0055's own audit found COLLAPSED; the exclusion holds in the tree either way.
THREE STALE COUNTS + ONE DEAD FIELD NAME, none load-bearing: ScenarioVM.gd:1025 and :1137 both say 'the three Unit fields' beside the two reset_all call sites (it resets four); dec.
8 says five structures (six); ScenarioDialogueBoxPool.gd:529 says 'the base lives in the VM's _unit_move_home', a field THIS ADR deleted -- behaviour right, address dead.
GUARD GAPS: dec.
1 as a negative (zero occurrences of the five old dict names in src/scenarios/ -- that IS 'fully internal', the regression is a one-line re-add, and the ADR warns about the getter-only-forward member-drop gotcha a forwarding property would reintroduce); dec.
7 as a COUNT (reset_all has exactly two call sites in src/ -- a third path is the literal shape of this bug's return and nothing would notice).
Dec.
8 must NOT be mechanized until the question above is answered.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

Unusually well covered already: fifteen `ScenarioActorTest` arms reach all four
verbs, both lifecycle concepts, both halves of the iid guard, and the rejected
symmetry. Two arms are absent:

- **Dec. 1 as a negative:** assert the five old dict names have zero occurrences
  in `src/scenarios/`. That is the entire content of "fully internal", the
  regression is a one-line re-add, and the ADR explicitly warns about the
  "getter-only-forward member-drop gotcha" that a forwarding property would
  reintroduce.
- **Dec. 7 as a count:** assert `reset_all` has exactly two call sites in `src/`.
  The bug this ADR fixes was *three paths clearing different subsets*; a third
  call site — or a second path that resets by hand instead — is the shape of its
  return, and nothing today would notice.

Dec. 8 is the one item that should **not** be mechanized until the question above
is answered — a guard on "the actor has six fields" would pin whichever reading
the guard's author happened to hold.
