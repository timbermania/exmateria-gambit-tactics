# ADR-0059 — audit notes

*Verified 2026-08-28. ADR-level verdict: `complete`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

VERDICT SCOPE: 'complete' = all EIGHT decisions conform in code AND both rollout phases landed, including the one scheduled last; what rotted is three counts and one consequence's wording.
Numbered 8 Decision items 2026-08-28 (1-4+8 from the opening paragraph, 5-7 the three sub-decisions); dec.
anchors 1-8 added, amendment dated not numbered.
MEASURED: EventInstruction.gd has 176 members with 176 UNIQUE NAMES and 176 UNIQUE VALUES, value == opcode byte.
event_opcodes.json is GONE; event_instructions.json holds 176.
run_all_tests.sh:91 runs gen_opcode_catalog.py --check and :110 the slug-rule unittest, so dec.
2's '--check-guarded like activity_taxonomy' is literally true.
75 _bind + 5 explicit _skip; BLOCK_END:828 / EVENT_END:832 / EVENT_END_2:833 are ordinary binds and NO name-string dispatch remains.
PHASE 2 IS DONE: _params_dict has ZERO definitions or call sites in src/, surviving only in docstrings about what it used to do.
Dec.
5 reproduces exactly: 40 UNKNOWN_0X** members and precisely VARIABLE_LE/GE/EQ/NE/LT/GT for the six Variable ops.
Dec.
7 has BOTH arms: ScenarioEventInstructionCoverageTest asserts it and ScenarioVM:966 push_warnings the same set at boot.
THREE COUNTS DRIFTED, NO MECHANISM DID: 'the 48 Unknown entries' is 40 today (eight RE'd and named), '45 Scenario*Test nets' is 83, verified:true is 27.
ScenarioVM.gd:925 repeats the stale 48 in a comment -- but the code under it loops EventInstructionSet.unknown_opcodes(), a catalog QUERY, so the number is wrong in two places and load-bearing in neither.
Same shape in the coverage gate: assert is a literal 27, its docstring opens 'carries 25 verified:true today' before a comment trail that adds to 27.
ONE CONSEQUENCE'S WORDING IS CONTRADICTED BY ITS OWN IMPLEMENTATION: the ADR condemns BG Sound's 'hand-counted positional decode' and promises it 'is retired on its migration turn'; ScenarioVM.gd:2016 opens '{6B}/{6A} operands are read POSITIONALLY through the reader, not by name' -- deliberately, because the catalog misnames op[1] 'Echo' (it is the ramp StartVol) and gives {6A} TWO operands both named 'Unknown', which _params_dict collapsed, losing one.
RECORDED QUESTION: was it retired? (a) yes in substance -- the objection was hand-counted decode outside a typed contract, and reads now go through EventInstructionArgs with catalog widths/types and PsxNum sign extension, with the dup-name loss impossible; (b) no -- the ADR's NAMED harm is 'breaks if the disassembler reorders operands' and nth is exactly as reorder-fragile as op[0]; only the layer doing the counting changed, so the honest fix is to correct the catalog names for {6A}/{6B}.
BOTH HALVES OF THE DEFERRED SIBLING HOLD, in opposite directions: battle_conditional_opcodes.json is still NOT renamed, AND the pattern was adopted on its own -- BattleConditionalOpcode.gd is generated by the same gen_opcode_catalog.py, with BattleConditionalSet + its test beside it.
EventInstructionSet is still in src/scenarios/, the defended departure from XDatabase.
GUARD GAPS: dec.
4 as a negative (zero _params_dict definitions in src/ -- the retirement is currently guarded only by its own absence); dec.
3/8 as a negative (no _handlers key is a String -- true today by convention, not by check).
NOT worth guarding: the 'Unknown' count, which is SUPPOSED to fall as RE lands; delete the number from the two comments rather than assert it.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

This is the best-mechanized ADR the audit has reached. Dec. 2 has a `--check`
pre-flight, dec. 5 has a dedicated slug-rule unittest
(`tools/test_gen_opcode_catalog.py`), dec. 7 has both of its own arms, and dec. 4
is covered by `EventInstructionArgsTest` including the `{6A}` duplicate-name case
by name. Two arms are absent and both are one grep:

- **Dec. 4 as a negative:** assert `_params_dict` has zero definitions in `src/`.
  Phase 2 is complete, and nothing stops a future handler from reintroducing the
  flattener; the retirement is currently guarded only by its own absence.
- **Dec. 3/8 as a negative:** assert no `_handlers` key is a `String`. The whole
  point of dec. 1 is that a display string can never again do an identity's job,
  and today that is true by convention rather than by check.

A third arm is *not* worth writing: a guard on the "48 Unknown" count would pin
a number that is supposed to fall as RE lands — the drift-proof mechanism
(`unknown_opcodes()`) is already the right answer, and the fix for the two stale
comments is to delete the number, not to assert it.
