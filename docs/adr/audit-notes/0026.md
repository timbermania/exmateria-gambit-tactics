# ADR-0026 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Decision was a flat bullet list and the file had ZERO anchors of any kind; numbered 1-5 on 2026-08-28 (no citation could break) and Amendment 1 appended with a per-decision What-is-current table.
MEASURED: the invariant this ADR exists to protect HOLDS and is now structurally enforced, but two of the nouns it is phrased in are gone.
`_start_state_animation` -- the single legal setter the headline rule and the whole Context are written around -- DOES NOT EXIST (no `func _start_state_animation` in the tree; only comments recording its retirement at Unit.gd:861 and UnitDisplay.gd:279).
ADR-0053's Path-D refactor replaced it with UnitDisplay.play_body:200, and the only BODY-layer type1_playback.start() is now inside UnitDisplay.apply_resolution:293, keyed off current_anim_id.
That is a STRENGTHENING: the forbidden move is no longer merely absent, it is inexpressible, because the clock key is derived from the same current_anim_id write that updates the render field, in one function.
CONSEQUENCE FOR dec.
5: its argument no longer applies to its own assertion.
dec.
5 binds on current_animation_front rather than type1_playback.anim_id because the latter 'was correct while the bug was present'.
current_animation_front is now written in exactly one place (play_body:215, three lines after current_anim_id = anim_id and immediately before _arm_anim_id_clock()) and UnitDisplay.gd:210 calls it a back-compat mirror, so the two assertions CANNOT disagree -- the desync it was written to catch is unreachable.
The assertion is not useless (it still compares the running slot against last_resolution.body_slot, catching a DISPATCH error), but its stated discriminating power is gone; a reader following that rationale today is reasoning from a defect that was designed out.
dec.
1 IS NOW CONDITIONAL AND THE DEFAULT IS THE OTHER BRANCH: CombatLoop.celebrate_on_victory defaults to FALSE (:106), so a surviving winner returns to IDLE (resolver picks IDLE_LOW_HEALTH for a critical unit) and a KO'd winner returns LEAVE_ACTIVITY and stays a corpse; CombatLoop:1232 intercepts before ActivityTranslator and calls settled_victory_activity(celebrate, is_dead) (:1207).
The GPU win handshake is untouched -- stage_victory still keys on LOGICAL_ACTIVITY_CELEBRATING; only the pose forks.
The dance is now opt-in: GPUCombatTestBase:116 and the navigator debug panel (NavigatorMain.gd:1287, ScenarioDebugSession.gd:52) opt in; production defaults faithful.
dec.
5's net is therefore NOT vacuous (the suite running it forces the branch) but it guards a non-default behaviour, and the opposite arm has its own guard (CombatFaithfulPoseTest fails if a winner is still CELEBRATING with the dance off); CombatVictoryPoseTest:62-77 pins settled_victory_activity as a pure function on both flags.
VERIFIED GOOD: dec.
2 exactly (resolve_celebrating:170-174; map.tres Resource_celt1 and Resource_celt3 are both front=82/back=83), dec.
3 (dispatch is ActivityTranslator.translate at CombatLoop.gd:1258; the victory branch sets an activity and returns, no clock poking), dec.
4 word for word (override at the _update_unit_animation call site, BEFORE the dead and _spell_cast_active guards, and it clears the latch at :1233), and dec.
1's MISS rule -- map.tres has 10 IDLE rows and only 2 CELEBRATING rows, which is the explicit-MISS decision working, not a gap (dec.
5's net requires the atlas binding only for TYPE1/TYPE3 while requiring activity == CELEBRATING for all).
The hardcoded '58' is gone from CombatLoop.
CLOSES AN OPEN QUESTION from ADR-0024's audit: this ADR does not ratify the ADR-0001 dec.
2 exception, it CAUSES it -- its References make tools/activity_taxonomy.yaml the authoritative Logical<->Display mapping, and tools/gen_activity_taxonomy.py emits from that YAML both the GDScript block (GPUConstants.gd:60-77) and a GLSL block (:233), which is exactly the 'hand-authored schema file that emits both sides' ADR-0001 dec.
2 rejected.
ADR-0001 dec.
2 already carries the pointer ('See Amendment 1: one constant family now works the rejected way') and this is that family, so nothing changes on either file.
No ambiguity left unresolved on this ADR.
