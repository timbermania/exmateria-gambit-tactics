# ADR-0071 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Numbered the 3 bold Decision paragraphs (### headings, prose untouched) and appended Amendment 1.
Anchors [[],[],['1','2']] -> [['*','1'], 1-3, 1-3].
Status still says 'proposed, build to follow via TDD' - third ADR in a row audited whose Status is stale (cf.
0040, 0069).
It is built PAST this design: InspectorProjectorRegistry knows 11 target kinds and src/effects/studio/ holds 16 projector files where the ADR names four; 4 later ADRs (0073/0075/0086/0087) and 15 source files cite it.
Dec.
1 HOLDS and was generalized - SpanProjector and EmitterTargetProjector are separate registered kinds, InspectionTarget={kind,ref} is real (ADR-0073's banner grades ACCURATE), EmitterProjector.gd:56-59 emits the 'shared by N events' note citing this ADR and EffectKeyframeInspector.gd:549 renders it; the signal went FURTHER than asked (EffectScoreModel.gd:1541 made the emitter header a clickable reverse-navigation row, fixing a misleading 'shared by 0 events').
Dec.
2 HOLDS EXACTLY - ScreenTweenProjector.sections() :46/:55 returns disjoint Blend vs Gradient field sets (absent, not greyed) and summarize() :65-68 gives label+border+fill as the Consequence describes.
Dec.
3 is HALF: the half that matters landed - _span_label() is GONE from EffectScoreModel, so the god-switch the ADR existed to remove is deleted - but summarize() is defined on 3 of 16 projectors and CALLED ON 1.
ScreenTweenProjector.summarize has EffectScoreModel.gd:512 + ScreenTweenProjectorTest.gd:68-69; CameraTweenProjector.summarize :135 and PaletteTweenProjector.summarize :64 have ZERO callers in src/ AND tests/; EmitterProjector and SoundTriggerProjector - two of the four the ADR names by hand - never got one.
So 'one projector owns both outputs, so lane and inspector share one source of truth' is load-bearing for the SCREEN archetype (the one with the visible bug) and unrealized elsewhere.
NOT the rejected status quo though: the surviving _kind_label() :1281 fills an inspector row VALUE at 2 call sites, and the labels at :397/:540/:649/:774/:958 are LANE headers, not per-event.
RECORDED QUESTION, not resolved: delete the two dead summarize()s or wire them? Dead-because-unneeded - camera/palette lanes draw colour spans with no per-event label and nothing ever asked for a summary, so they are speculative generality from following the ADR's shape literally.
Dead-because-unfinished - those lanes DO present a fill (:649/:760/:774) computed in EffectScoreModel rather than by the projector, which is exactly the drift dec.
3 exists to prevent.
Deciding needs a read of ADR-0086/0087's territory, not this one's.
Two further measurements: the {FADE,TINT} retirement landed at the TYPE level (ScreenData.ScreenMode is BLEND/GRADIENT) with a deliberate exception the ADR did not distinguish - the strings survive as ON-DISK labels via parse_mode/mode_label :24-31 so a written screen.json round-trips, and EffectScreenSaver.gd:74 writes 'FADE'.
And BOTH forecasts missed their mechanism: TimeScaleProjector has zero hits and a lane render-mode flag has zero hits - time-slow landed as TimeScaleChannel + a _pacing_band() :311 band - though the forecast's CLAIM (the seam would not need reopening) is true.
'Today the Studio is read-only' is also expired (41 studio_* verbs, 15 *Saver.gd files); whether the shared-by-N note became the promised live fan-out warning belongs to ADR-0075's audit.
Guard feasibility: dec.
2 is already guarded end to end (ScreenTweenProjectorTest) and dec.
1's note by EffectKeyframeInspectorTest.
Dec.
3 is the gap and the arm is cheap - assert every src/effects/studio/*Projector.gd that defines summarize() has a caller; that arm would have surfaced the two dead methods instead of leaving them to an audit.
The stronger arm ('every archetype's lane presentation is computed by its projector') cannot be written until the recorded question is answered, because it presumes the second reading.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

Dec. 2 is already guarded end to end — `tests/ScreenTweenProjectorTest.gd` calls
both `summarize()` branches and the disjoint `sections()` sets. Dec. 1's fan-out
note is covered by `tests/EffectKeyframeInspectorTest.gd`.

Dec. 3 is the mechanizable gap, and the arm is cheap: assert that every file
matching `src/effects/studio/*Projector.gd` which defines `summarize()` has a
caller. That is the arm that would have surfaced the two dead methods above
instead of leaving them to an audit. The stronger arm — "every archetype's lane
presentation is computed by its projector" — cannot be written until the
recorded question is answered, because it presumes the second reading.

Verdict recorded as **half-landed**: dec. 1 and dec. 2 are built and generalized,
dec. 3's god-switch removal landed while its one-source-of-truth property reaches
one archetype of four.
