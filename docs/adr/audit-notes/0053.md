# ADR-0053 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Numbered the 6 Decision bullets (additive; anchors were [[],[],[]] — nothing in this ADR was citable) and appended Amendment 1.
Well covered: 8 tests cite ADR-0053 (UnitCurrentAnimIdTest, GetPoseOctantTest, CinematicPoseLUTTest, UnitDisplayPaintGoldenTest, UnitCinematicIdleModeTest, UnitThrowBodyDispatchTest, ScenarioFacingAnimDecodeTest, UnitEffectCleanupOnStateChangeTest).
Every rule holds; three moved.
dec.
1: the field is on UnitDisplay.gd:67, NOT Unit — Unit.gd:345 is a read-only forwarding property, the write half retired under #152 so there is no setter; play_body() is the single funnel and it re-arms the clock.
A STRENGTHENING (a driver can no longer assign and forget the clock), but 'one field' is one and a half: play_body also repopulates current_animation_front/index (:215-216), still read at Unit.gd:1066.
dec.
2: CombatLoop does not write the field — it calls unit.update_animation(), which ends at display.play_body(r.body_slot) (Unit.gd:904-924).
dec.
3: _op_unit_anim (:3923-3927) is decode/apply/verb, not a direct assignment, and there is a SECOND event-side writer the ADR does not name — {8C} _op_unit_anim_rotate (:4061-4063), documented at :794-796 as mirroring {11}'s animation path.
dec.
4 holds in full (body_back_slot has ZERO hits).
dec.
5 holds and the push_error stub is GONE (EVTCHR branch real at :390-397) though the docstring one screen above at :330 still calls it a stub.
dec.
6 holds plus AnimationStateController.pose_octant_to_atlas_cardinal (:140) = (pose_octant >> 2) & 0x3 — the ADR's bare 'pose_octant >> 2' is missing the mask.
AMBIGUOUS, recorded not resolved: the Decision table says SEQ-range 1..0x1f4 / EVTCHR >= 0x1f5, but the code branches `< 0x1f4` (UnitDisplay :287/:376) so 0x1f4 itself takes the EVTCHR branch.
Reading (a): the ADR is off by one and the later PSX band decode is right — :390 documents a mid band [0x1F4,0x258) and a high band [0x258,..), and the ADR knows only the high one.
Reading (b): the code is off by one and `<` should be `<=`.
No test exercises anim_id == 0x1f4.
The tell: the SAME file carries both spellings (:323/:330 and AnimationResolutionMap.gd:30-31 restate the ADR's boundary).
VIOLATED: the naming Consequence ('the event-script writer path must not be called cinematic anywhere in code or docs').
20 files under src/animation/+src/scenarios/ use the word across 18+ identifiers (CinematicPoseLUT is a class_name with its own test, CinematicWalkState, is_cinematic_unit, cinematic_seq, _play_cinematic_unit_anim, ...) while ADR-0037's owner is src/gpu/CinematicManager.gd.
Not late drift — the ADR's OWN Out-of-scope entry names ScenarioVM.CinematicWalkState.
Guard feasibility: dec.
1's funnel is the highest-value arm (assert no .gd outside UnitDisplay.gd assigns current_anim_id — Unit's property is getter-only, so any assignment is dead or a second write path); dec.
4 is trivial (assert body_back_slot count 0).
The naming arm would land PERMANENTLY RED on 20 files, so it is a decision (retire the rule or schedule a rename), not a guard to file.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

Decisions 1, 4, 5 and 6 already have direct test coverage (see the eight tests
above). Three arms are missing:

- **Decision 1's funnel is mechanizable and is the highest-value arm.** Assert
  that no `.gd` outside `UnitDisplay.gd` assigns `current_anim_id` — the property
  on `Unit` is getter-only, so any such assignment is either dead or a second
  write path. This is a cheap text net over the walk and it locks in the
  strengthening that issue #152 already landed.
- **Decision 4 is trivially mechanizable**: assert `body_back_slot` has zero
  occurrences. Today it does; nothing stops it coming back.
- **The naming Consequence is mechanizable and would land RED** — an arm
  asserting no `cinematic` identifier outside `src/gpu/CinematicManager.gd` fails
  on 20 files. That makes it a *decision*, not a guard: either the rule is
  retired as lost (and the Consequence amended to say so), or a rename is
  scheduled. Filing the guard before that call would be filing a permanent red.
