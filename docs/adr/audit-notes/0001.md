# ADR-0001 — audit notes

*Verified 2026-08-28. ADR-level verdict: `complete`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Decision numbered 1-9 on 2026-08-28 — the ADR had NO `## Decision` section at all, so the rules were lifted from Considered options / Consequences with a pointer each and nothing retired.
OPEN QUESTION, recorded not guessed: decision 2 rejects a neutral schema file, but `LOGICAL_ACTIVITY_*` is generated from `tools/activity_taxonomy.yaml` into BOTH sides including a GENERATED region inside `combat_common.glslinc`, so for that family the shader is a consumer.
ADR-0026 calls the YAML authoritative — in its REFERENCES section, citing neither ADR-0001 nor the inversion — so either it ratifies the reversal or it ratified only the Logical<->Display mapping and the reversal is unrecorded.
MEASURED SEPARATELY: all 18 non-ADR citations of `ADR-0001` in godot-learning (6 src, 2 tests, 10 tools) mean the ROOT `0001-iso-derived-assets-reproducible.md`, not this ADR — the register's code/test/tool counts for 0001 are entirely a number collision, and check_adr_anchors cannot see it because a bare `ADR-NNNN` has no anchor.
The mechanism itself is enforced (gen_gpu_layout.py --check at run_all_tests.sh:70 + the boot validator), though `guard` reads — because that column only matches `check_*.py`.
Two Consequences facts are stale: the generated target moved to `GPUCombatPacker.gd` (gaining `BattleHeaderField`/`BATTLE_STATE_FIELDS`, with the 195 `UnitField.X` refs preserved by a forwarding const), and `STATE_NAMES` was deleted by #46.
