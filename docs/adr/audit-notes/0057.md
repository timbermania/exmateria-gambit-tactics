# ADR-0057 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Had NO `## Decision` heading, so `ADR-0057 dec.
N` resolved against nothing; renamed `## The three classes` -> `## Decision: the three classes`, which promotes its existing `### 1./2./3.` items to decision anchors (dec.
1 Placement, 2 Orientation, 3 Render) matching the numbering already on the page.
Nothing renumbered; 93 citations to this ADR outside the generated rows and none names a section or decision.
Dec.
2 and dec.
3 hold, and dec.
3 is GOLDEN-LOCKED: three named converters in one module (AnimationStateController :97/:125/:140) pinned at the cardinals and between-cardinals by tests/RenderCardinalConverterTest.gd (#138), which cites this ADR in its first line; it and ReferenceRenderDirectionTest are both in run_all_tests.sh (:1889, :1894) and neither is skipped.
Dec.
2 is now literally true — PSXCameraConvert.gd:59-60 dropped the -360.0 no-op for the identity.
The affine corollary holds (extract_event.py:119-123, mesh.py:247-254).
TWO MOVES.
(1) Dec.
1's 'lone transitional holdout' CLOSED: #141 folded the chunk flip into extract_event._flip_placement_rows:126-149 (stamps _placement_flipped/_map_size_z :212-215) and ScenarioVM._flip_depth_row is gone, meeting the paragraph's own conditions including the .raw.json RE sidecar; recorded only at tools/parse_scenarios.py:93-103.
ADR-0052:71-72 and docs/context/23-*.md:16-20 are still on the pre-#141 text, and the context cluster is the copy this ADR's Status calls canonical.
(2) A Placement this ADR never classified IS flipped at runtime: the {19} Camera opcode's X/Y/Z position appears in neither dec.
1's enumeration (:25-26) nor dec.
2's (:58-60, which covers only pitch/yaw/roll), and ScenarioCameraDirector.gd:750-752 mirrors the camera body depth via flip_depth_continuous, gated on camera_flip_body_depth (:160, default true, NO writer), verified to the pixel (F19, :142-158).
So dec.
1's end-state sentence is false today and the exception it names is the wrong one.
RECORDED QUESTION, not resolved: is the camera body an ordinary Placement (then the flip moves into extract_event beside Warp/Walk To, the runtime branch is deleted, and F19's parity must be re-established on regenerated data) or a genuine RUNTIME Placement (the pose is VM-interpolated per frame, so there is no static chunk value to pre-flip — which is why flip_depth_continuous exists at all, PsxNum.gd:146-152 — and dec.
1 needs an explicit fourth sentence rather than a silent exception)? Evidence leans to the second but the ADR is silent.
Two Consequences moved: camera_position_offset was DELETED (#139, four tests assert its absence) though tools/diag_scenario_screens.gd:42-48 still writes it; and 'Render is the only class still carrying real debt' (:140-142) is now the document's weakest claim — Render's three numberings are named, converted through one module and golden-locked, while Placement carries the unclassified camera body.
The historical-bug list grades 4 for 4 against the tree.
Guard column —; three arms named, arm 1 (flip_depth_row/flip_depth_continuous have no src/ callers outside an allowlist that is either empty or exactly the camera-body site) turns dec.
1's end-state into a fact but should be written AFTER the recorded question is answered, since the allowlist IS the answer; arm 2 (no committed chunk carries _placement_flipped false) serves this ADR and ADR-0052 both.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

The guard column is `—`. Dec. 3 is already effectively guarded by
`RenderCardinalConverterTest`. The two unguarded halves are mechanizable:

1. **No Placement flips at runtime.** Assert that `PsxNum.flip_depth_row` and
   `flip_depth_continuous` have no callers in `src/` outside an explicit
   allowlist, and that the allowlist is empty (reading 1) or holds exactly the
   camera-body site with a comment citing this ADR (reading 2). This is the arm
   that turns dec. 1's end-state sentence from prose into a fact, and it is the
   arm that would have flagged the camera body.
2. **Every committed chunk is pre-flipped.** Assert no `scenario_*_chunk.json`
   carries `_placement_flipped: false`, and that its `_map_size_z` matches the
   map's `terrain.json`. `extract_event.py` already emits both fields; nothing
   reads them back. (Named identically in `docs/adr/audit-notes/0052.md` — one
   arm serves both ADRs.)
3. **Not mechanizable: the classifier itself.** "Sort a new quantity into
   Placement / Orientation / Render on day one" is a review discipline, not a
   predicate. The honest substitute is arm 1: it does not check that new
   quantities are classified, but it does make the one class boundary that has
   actually leaked twice impossible to cross silently.

Arms 1 and 2 are worth writing. Arm 1 should be written **after** the recorded
question above is answered, since its allowlist is the answer.
