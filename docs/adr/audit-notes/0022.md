# ADR-0022 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Decision was a flat bullet list with zero decision anchors; numbered 1-8 on 2026-08-28 (no citation could break) and Amendment 1 appended with a per-decision What-is-current table.
MEASURED: dec.
2 ('BODY layer only; WEAPON and EFFECT stay RGBA-baked') is SUPERSEDED IN CODE and its stated reason is the part that was wrong -- it argued 'neither has a per-unit palette-row concern', and unit_sprite_body.gdshaderinc carries wep1_palette/wep1_palette_row (:652/:656) and eff1_palette/eff1_palette_row (:657/:661) driven by BATTLE.BIN's per-item table at 0x2d3e4 (weapon_graphic_data.json, X nibble -> wep1, Y nibble -> eff1), which is exactly the per-equipped-item axis dec.
2 named for WEAPON before dismissing it.
The pattern also travelled to TRAP1 (effect_particle_stp.gdshaderinc:33, TrapEffect.gd:254), projectiles (Projectile3D.gd:222/:641), range-tile overlays (tile_overlay.gdshaderinc:8/:13) and EVTCHR (bake_evtchr_textures.py), all citing this ADR.
dec.
5's body-row SOURCE is superseded too: the Consequences say 'humanoid jobs leave it at 0', but the shipped rule is two-axis (SpritePaletteResolver.resolve_body_palette_row:80 <- ScenarioPlayerScene:786) -- MONSTER takes the job's body_palette_row (SCUS 0x2E) and IGNORES the ENTD palette byte (real-PSX ground truth: scenario-6 unit 0x8B, job 0x5E, renders Yellow despite palette:2), HUMAN/NAMED takes the ENTD byte CLAMPED to rows the SPR authored via assets/sprites/populated_rows.json (Delita 0x05 authors {0,1} so byte 2 clamps to 0).
Pinned by ResolveBodyPaletteRowTest.
One Consequence is simply FALSE: '--portrait-palette is retired' -- it is still declared at extract_all_sprites.py:213 (default 8), threaded at :101 and baked at extract_spr.py:502/:516; what is true is only that it no longer reaches the BODY path (extract_body_sprite:71-77 calls extract_spr_indexed with no palette arg), so it survives on the WEP/OTHER branch where the decision does not apply.
The Consequences' predicted uniform names never existed on the unit path: body samplers are type1_tex + type1_palette, portrait's are sprite_texture + sprite_palette (unit_portrait_3d.gdshader:12-13); palette_texture IS live but on the effect/projectile path (EngineFoldCompositor.gd:196, Projectile3D.gd:232), so grepping the ADR's noun lands on the wrong subsystem.
dec.
1's location noun is stale -- the uniforms and paletted sampling are in unit_sprite_body.gdshaderinc, and unit.gdshader is a 4KB shell that includes it (:32).
VERIFIED GOOD: dec.
3 (no unit shader reuses indexed_color.gdshader; cited only as a convention at :898), dec.
4 (extract_spr.py:453-454 writes the pair, named by extract_all_sprites.py:71-77; index packing confirmed by event_asset_slicing.py:32 INDEX_SCALE=17), dec.
5's uniform defaults (body 0 at :644, portrait 8 at unit_portrait_3d.gdshader:14), dec.
6 (ZERO GDScript assigns portrait_palette_row, so the known-broken-in-v1 limitation is still exactly true and it did NOT become the TacticsG stub-that-does-nothing trap the ADR feared -- there is no unassigned field at all), dec.
7 (extract_fft_data.py:293-294 emits the renamed keys; male_palette/male_portrait survive only as local vars and in the ROM offset comment block, which is the ROM's own field naming, not drift).
OPEN QUESTION, recorded not guessed: was dec.
2 a migration SEQUENCE ('BODY first') or a standing SCOPE limit? Read as sequence it held; read as scope -- which is how it is written -- it is superseded.
Its own justification argues scope, and the code contradicts the justification rather than merely the boundary, so the two readings give different verdicts for that decision.
Low-stakes second finding: ScenarioPaletteResolutionTest.gd's docstring still describes the retired V14 mechanism (unit.body_palette_row = slot['palette'] written at spawn); its chapel fixture is all-human with palette:0 so the test still passes under the two-axis rule and only the mechanism sentence is stale.
GUARD FEASIBILITY (rule 3, recorded since this row now carries a note and leaves that pool): mechanizable arms are a sibling check that every ^[0-9A-F]{2}\.tga$ under assets/sprites/textures/ has a *.palette.tga (dec.
4), a no-male_palette/male_portrait-as-jobs.json-key check (dec.
7), and a declaration check that unit_portrait_3d still defaults portrait_palette_row to 8 (dec.
5).
dec.
2 is NOT mechanizable because the code deliberately violates it, and dec.
1/3 are judgements, not predicates.
The one worth building is the dec.
4 sibling check -- it is the only rule whose breakage is SILENT (a missing sidecar renders transparent; UIPortrait.gd:386 documents that non-fatal path) -- but it needs the asset-presence skip, since assets/sprites/textures/ is gitignored and EMPTY in this documentation worktree.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

A text guard is possible and cheap for the parts that are file-shaped: assert
that every `.tga` under `assets/sprites/textures/` matching `^[0-9A-F]{2}\.tga$`
has a `*.palette.tga` sibling (dec. 4), that no emitter writes `male_palette` /
`male_portrait` as a **jobs.json key** (dec. 7), and that `unit_portrait_3d`
still declares `portrait_palette_row` with default 8 (dec. 5). Two of the eight
are not mechanizable as written: dec. 2 because the code deliberately violates
it, and dec. 1/3 because "the unit shader has its own idiom" is a judgement, not
a predicate. **The one worth building is the dec. 4 sibling check** — it is the
only rule here whose breakage is silent (a missing palette sidecar renders as a
transparent sprite, and `UIPortrait.gd:386` explicitly documents that
non-fatal path). Note that this cannot run in a checkout without the
ROM-derived assets populated: `assets/sprites/textures/` is gitignored and is
empty in this documentation worktree, so the guard needs the same
asset-presence skip the other extraction-dependent checks use.
