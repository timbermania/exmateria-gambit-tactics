# ADR-0181 — audit notes

*Verified 2026-08-28. ADR-level verdict: `complete`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Six Decision bullets numbered (anchors [] -> amd ['*','2026-08-28'], dec/sec 1-6).
Every decision holds three days after the build.
dec.
1: NavigatorMain:360/:364 declare both path constants and the old single name has 0 occurrences.
dec.
2: signal dismissed() :200, emit :2728 behind both gates.
dec.
3/4: the state gate and the Host.MAP gate are present WITH their reasoning in the source.
dec.
5: _resolve_roster and _unlock_every_job have ZERO occurrences in src/; FormationDevBoot.gd + assets/scenes/FormationDev.tscn exist and classify_blueprint.py:243 books the boot `assembler` citing this ADR.
dec.
6: still no Focus anywhere in src/ or addons/ — a live deferral, not a quiet build.
Consequences: ROOT_SET.tsv rows 9/10 declare both scenes (row 9 cites 0181), the two ADR-0143 dec.
3 KNOWN entries sit at check_root_set.py:168/:177, and the guard is GREEN (13 roots, 32 scenes declared).
FormationHandBackTest carries exactly the three arms it claims (_a_rest_hands_back/_b_open_screen_does_not/_c_map_host_does_not).
THE FIXTURE CENSUS GREW AND THE INVARIANT HELD: 34 of 36 became 37 of 37 — one test joined the set afterwards and stated its own fixture without being told.
ONE CONSEQUENCE IS NOW CLOSED BY ANOTHER ADR: 'Filed, not built: the world map has no system' is answered — classify_blueprint.py:245 books the directory per ADR-0176/#580 (WorldMapScene assembler; WorldMapProgress + WorldMapVariables Campaign; WorldMapMusicPort Audio; src/world_map/ -> UI residual).
This ADR's leaning that the bucket was Campaign was HALF right, which is exactly why refusing to book WorldMapScene.gd alone was correct.
Its measurement has drifted (15 files / 4,496 lines then; 14 .gd totalling 5,195 lines plus two shaders now) — drift, not error.
First ADR in 26 audits whose Status is not merely accurate but SPECIFIC (build date, three owning files, the guarding test and its arms, four dependencies) with every claim checking out, and whose Built section records two build mistakes rather than only the outcome.
Guard arm identified for dec.
5's negative half: assert no file under src/ui3/formation/ outside a *Boot.gd calls reset_to_new_game() — writable today, would pass, and it guards the actual finding (the fixture living on the player's route, not the fixture itself).


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

Dec. 2/3/4 are mechanized by `FormationHandBackTest` with both gates seeded red, dec. 1 by the
two navigator route tests, and dec. 5 by `check_root_set` + `classify_blueprint`. The arm that
is **absent** covers dec. 5's negative half: nothing asserts that `reset_to_new_game()` stays out
of a *screen* — the call is legitimate in `ProgressionDebugPanel` and `NavigatorRunner`, so the
assertion has to be scoped ("no file under `src/ui3/formation/` outside a `*Boot.gd` calls it"),
which is writable today and would pass. That is the guard against this ADR's actual finding: the
defect was never the fixture, it was the fixture living on the player's route.
