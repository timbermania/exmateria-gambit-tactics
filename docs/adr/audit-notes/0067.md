# ADR-0067 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Numbered the 6 Decision bullets (additive; 0 decision anchors before) and appended Amendment 1 grading each against the shaders, ColorStack and the tests.
HOLDS: dec.
1-3, 5 and dec.
6's owner half.
psx_color_apply(vec3 base, int surface_id) is exact; MAX_COLOR_LAYERS=8 in both the include and ColorStack.gd:22; no compose-op enum exists; ColorStack carries every duty dec.
6 lists (push_layer:118, push_op:147, _restore:165, evaluate:201, commit_bake:286, apply_packed:368).
The seam guard tools/check_color_shaders.py (run_all_tests.sh:309) ran GREEN, and its BANNED_UNIFORMS tuple ENFORCES the promised deletions (unit_tint_scale/bias, field_tint_*, the luma pairs, unit_tint, map_tint).
MOVED: dec.
1's path — the include + ColorStack.gd + ColorRecipe.gd now live in addons/exmateria_schema/colour_model/, not assets/shaders/ + src/.
GENERALIZED: dec.
4 gives the source={current,base} axis to LUMA only; ColorRecipe.affine_base() exists and the include's meta comment (:40) reads 'recipe source (0=current,1=base; affine & luma)'.
WRONG DRIVER NAMED: dec.
6 says EffectTimeline becomes a thin driver calling push_layer; it calls NONE of the model, by ADR-0014's design (output-agnostic, subsystems self-deliver).
The drivers are PaletteSubsystem/ScreenSubsystem/ScreenEffectOverlay + ScenarioVM + MapComposer + MapIlluminationDDA.
Intent landed, node misnamed.
REFUTED: the 'Deferred combat unification' claim that the screen param<<1 is spurious with no ROM counterpart, and that the effect-editor's DOUBLED help text is wrong.
086f871e4 (2026-07-14, three days after this ADR's Task B removed it) RESTORED the doubling — 'proven live on E173/savestate9', byte-exact fold at frame 53 — and corrected two E005 tests that had been COMPUTED un-doubled.
The ADR had ruled that live A/B 'optional...
not a blocker — the disasm is unambiguous'; it is the check that overturned the claim.
The doubling returned not to _calculate_blend_target (deleted with the imperative stepper in Task C, absent tree-wide) but as an opt-in recipe flag at one call site, ScreenSubsystem.gd:103-107 — i.e.
the unification survived, only the empirical claim died.
No amendment recorded it; ADR-0087 builds on double_param as settled and cites this ADR under Relates to without noting the reversal, and SpacerVerdicts.gd:30 encodes it as SCREEN_PROFILE with a FOURTH profile axis (clamp) beyond the three the Consumer-profile design rule names.
STALE: the Status section still says the combat 11-mode reductions 'still live outside the stack' while Task A/B below it report both IMPLEMENTED; guard counts 16/10/14 are all historical (measured 24 funcs-44 asserts / 4-12 / 16-35, unit unstated, all grown).
ScenarioVM.gd:2204 and :2308 still describe the DELETED unit_tint_scale/bias uniforms as the live mechanism and name MapComposer.set_field_tint, which exists only as a test stub (ScenarioCommitPaletteTest.gd:52); production has set_field_color_stack/commit_field_tint/bake_field_tint.
AMBIGUOUS, recorded not resolved: the same paragraph, same day, same instrument (live Ghidra decompile + xrefs, no emulator) refuted BOTH the effect-editor's x2 and its x8 map-scale annotation.
The x2 is now known wrong.
Does the x8 refutation stand independently (reading A: it is a STRUCTURAL claim — no third applier, xref-enumerated — and the <<3 sightings are the x7 descriptor stride) or inherit the doubt (reading B: same sitting, same confidence sentence that failed, and an x8 on the param would render as fraction-strength content no headless test can see)? Settling it needs one live map-tint capture, the shape of 086f871e4's E173 run.
The help text still carries both claims uncorrected (color_tracks_tab.lua:47/485/507).
Guard feasibility: three arms named — a BANNED_UNIFORMS-over-prose arm (RED today at ScenarioVM.gd:2204/:2308, needs an epitaph allowance for the two deliberate shader mentions), a dead-reference arm (MapComposer.set_field_tint), and a profile arm asserting double_param=true reaches only screen-lane call sites (GREEN, pins the correction).


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

The seam itself is netted (`check_color_shaders.py`, green, with the banned-uniform
list as its teeth) — this is one of the better-guarded ADRs in the corpus. Three
arms are possible and unwritten:

1. **A docstring arm.** The banned-uniform check reads shaders only. The same
   `BANNED_UNIFORMS` tuple applied to `.gd` prose would be red today at
   `ScenarioVM.gd:2204` and `:2308`, which describe deleted uniforms as the live
   mechanism. It would need an epitaph allowance — the two shader mentions above
   are deliberate and must stay.
2. **A dead-reference arm.** `MapComposer.set_field_tint` is cited from a live
   docstring and exists only as a test stub. A general "a `Class.method` named in
   prose resolves in `src/`+`addons/`" check is larger than this ADR, but this
   ADR supplies a concrete red case.
3. **A profile arm.** Assert every `push_op(..., double_param: true)` call site
   is a screen-lane consumer, and that no palette-lane caller passes it — the
   invariant `ScreenSubsystem.gd:105` states in prose ("the palette CLUT path
   does NOT double") and that `SpacerVerdicts`' two profile constants encode.
   Green today; it pins the correction that overturned this ADR's own claim.
