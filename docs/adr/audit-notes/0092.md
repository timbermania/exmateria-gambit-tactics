# ADR-0092 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Built, but the Status still says #272 /tdd pending — 4th audited ADR in a row with that defect (0040, 0069, 0071, 0092).
Six Decision items numbered; the dated Amendment heading became Amendment 1 (no citation addressed the date; anchors additive).
Dec.
2/3/4/6 and all five recipe artifacts grade built; dec.
4's deferral has since been paid (SoundContainerProjector edits in place, two shape:edit rows).
Dec.
5's reader half is built (EffectData.gd:218) but the 401-dir regen is UNVERIFIABLE in this worktree — assets/effects/ is gitignored ROM-derived content and unpopulated, so a zero count here refutes nothing.
HEADLINE: Amendment 1's removal half never landed — _flags_section() still declares masks 0x20/0x40 and EffectSettingsProjectorTest:183-196 ASSERTS all four bits, which is exactly what Amendment 1 forbids ('not on this Effect Settings surface at all'); ADR-0093:92 repeats the amendment's reading.
Not two writers (both controls address channel effect_flags, one choke point) — two CONTROLS.
RECORDED QUESTION: (A) finish the relocation — delete 0x20/0x40 from _flags_section, delete two green assertions and rewrite the manifest note; or (B) the tree is right and Amendment 1 + ADR-0093:92 overstated.
Product call, not guessed.
Also measured: the recipe's class_name justification was false when written — 5 of 16 *Channel.gd carry none, 7 of 12 *Saver.gd do, three of those predating 2026-08-13; the prediction for the two named files still held.
Guard arm deferred until the question is answered: assert masks 0x20/0x40 appear on exactly ONE of _flags_section() and the Time Scale pop-up (today: two).


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

Decisions 2, 3, 4 and 6 are already mechanized by `EffectSettingsProjectorTest`,
`EffectStudioEffectFlagsAcceptanceTest`, `EffectFlagsChannelTest`, `EffectFlagsSaverTest` and
`EffectEditabilityManifestTest` — this ADR is among the better-guarded in the corpus. The one
arm that is **absent and cannot be written until the question above is answered** is a guard
over the bit-5/6 *home*: whichever reading wins, the assertion is "mask `0x20`/`0x40` appear on
exactly one of `_flags_section()` and the Time Scale pop-up", and today it would report **two**.
Writing it now would only pin whichever answer the author of the guard happened to prefer.

## 2026-08-28 — blocked, and what the commit clock adds

The de-amendment pass took this ADR as the burn-down head and **stopped**: the skill's
"a question blocks the file" rule fires. The file was not rewritten. Both readings are in
`AUDIT.tsv` under dec. 1.

New evidence measured while trying to settle it — everything happened on **2026-08-13**, so
only the clock separates the events:

| Time (PDT) | Commit | What it did |
| --- | --- | --- |
| 09:21 | `b46682aa0` | built `_flags_section()` with **all four** bits, and wrote `_test_flags_row_declares_four_engine_bits` |
| 15:08 | `79a5d59c1` | wrote Amendment 1 — bits 5/6 "not on this Effect Settings surface at all" |
| 19:54 | `ceb7ae85e` | built the Time Scale pop-up enables (`_pacing_enable_bit`) — the **addition** half |
| 19:58 | `8fd14608f` | touched `EffectSettingsProjector.gd` *and* the four-bit test again (#273), leaving four bits |

So the amendment was written **between** the two builds, and the two commits that came after it
both touched exactly the files the removal would edit and left the four-bit group standing. That
is consistent with *either* reading — unfinished work, or a quiet decision to keep both controls —
which is precisely why it is not decidable from the tree. It does rule out one thing: the
amendment was not written in ignorance of the shipped four-bit group, which was already there.

Amendment 1 attributes its rationale to **the user's call** ("one control per concept, sitting
next to its curve"). Reading B therefore asks the author to revisit their own ruling in light of
what building it produced. That is the question, and it is theirs.
