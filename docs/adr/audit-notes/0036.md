# ADR-0036 — audit notes

*Verified 2026-08-28. ADR-level verdict: `half-landed`. Working input for this ADR's de-amendment rewrite; delete this file when the rewrite consumes it.*

Numbered the 8 Decision bullets (additive; 0 decision anchors before) and appended Amendment 1 grading each against the tree.
HOLDS: dec.
1, 3, 6, 8 — dec.
6 (Option A single-source) is the only one with a test, PSXDisplaySingleSourceTest.gd:32, and shader_global_default() at PSXDisplay.gd:105 is exactly as written.
MOVED: dec.
2's rule holds but its SPELLING is superseded — ADR-0060 put both patterns behind psx_par_full()/psx_par_anchor() in addons/exmateria_platform/pixel_aspect/psx_par.gdshaderinc, enforced by tools/check_par_shaders.py (102 walked, 11 BURN_DOWN), a guard that cites ADR-0060 not this ADR; psx_par_anchor also takes a third `stretch` arg (ADR-0044) Pattern 2 has no slot for.
dec.
5 moved three ways: five mirrors not two (ADR-0044 added cursor/unit/fx), all routed through Tune (ADR-0068) rather than direct setters, and DisplayDebugPanel is DELETED (ADR-0151, bb5f7fefa) — the scrub is a generated Tunables card.
Its `live_ui_par defaults to PAR` clause names PAR := 1.25, a constant ADR-0152 deleted as zero-reader.
FALSE: dec.
4's default.
project.godot [shader_globals] psx_par reads 1.0, not 1.25 — landed at 1.25 in 311dab22a (2026-06-10, this ADR's accept date) and moved to 1.0 in b7b336491 (2026-07-06 'alignment-workbench tuning'), a commit citing no ADR.
live_ui_par defaults to 1.0 too, so dec.
7's four 1.25 @export defaults are overwritten to 1.0 at runtime by dec.
8 and the canonical 1.25 is editor-preview-only.
dec.
7 half: the four defaults ARE 1.25 (UIChar:75/UIText:42/UIPortrait:44/UIFrame:123) but get_display_width() has ZERO callers tree-wide (2 definitions, 0 reads) — UIText inlines * pixel_aspect_ratio at :204/:251/:261/:286; its UIChar:154/UIPortrait:116 citations are stale.
AMBIGUOUS, recorded not resolved: is world PAR 1.0 a retirement of the canonical 1.25 (ADR-0152 deleted the constant, live_ui_par's comment flips it deliberately) or a workbench scrub that leaked (the commit moved several globals at once, cites no ADR, and left the UI @exports at 1.25)? A guard on dec.
4 must read the default, never assert a literal, until a human rules.
Guard feasibility: dec.
3 (PAR-blindness of CUSTOM0/object-space VERTEX) is mechanizable and unguarded — it survives only as prose in two shader comments; dec.
7's get_display_width clause is mechanizable and would be RED today.


## Harvested: On mechanizing this ADR

*Verbatim from the ADR body. The rewrite deletes the original; this is the copy.*

A guard already exists for decision 2, but it is **ADR-0060's**, not this
ADR's: `tools/check_par_shaders.py` walks `classify_blueprint.WALK_ROOTS` and
requires every POSITION-writing shader to `#include` the seam and write POSITION
from `psx_par_full(...)` or `psx_par_anchor(...)`, with `// psx-par-exempt:` the
only opt-out. Today it reports "102 walked, 11 on the BURN_DOWN list" — ten
`src/ui3/shaders/*` files plus `assets/shaders/unit_flat.gdshader`, each a
standing violation with a both-arms direction test on stale entries.

Two arms are missing and are cheap:

- **Decision 3 is mechanizable and unguarded.** Assert that no `.gdshader` /
  `.gdshaderinc` multiplies `CUSTOM0` or object-space `VERTEX` by `psx_par` or
  any `psx_*_stretch` global — the PAR-blindness invariant, which today survives
  only as prose in two shaders' comments.
- **Decision 7's `get_display_width()` clause is mechanizable and currently
  false.** Assert that the accessor has at least one caller, or delete the
  clause; a published width accessor with zero readers cannot be enforcing
  anything.

Decision 4's literal is deliberately left unmechanized until the open question
above is answered — a guard asserting `1.25` would be red on landing, and one
asserting `1.0` would ratify a value nobody chose in writing.
